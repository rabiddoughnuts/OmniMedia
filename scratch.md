# OmniMediaTrak Scratch Notes

## Overall structure and layout

-   Repo is a simple two-app layout: /api (Fastify) and /web (Next.js), with a minimal root README and a single Postgres schema.
-   API is already split into routes and middleware, with DB helpers and raw SQL migrations.